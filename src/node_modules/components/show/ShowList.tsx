import React from 'react'
import ShowCard, { ShowProps } from "./ShowCard"

type ShowListProps = {
    data:ShowProps []
}

const ShowList = ({data}:ShowListProps) => {
  return (
      <div>{
          data.map(item => <ShowCard key={item.show.id} {...item}/>)
      }</div>
  )
}

export default ShowList