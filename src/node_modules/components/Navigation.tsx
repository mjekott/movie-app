import React from 'react'

const Links = [
    { to: "/", text: 'Home' },
    {to:"/starred",text:"Stareed"}
]


const Navigation = () => {
  return (
      <div>
          <ul>
              {
                  Links.map(item => <li key={item.text}>{ item.text}</li>)
              }
          </ul>
    </div>
  )
}

export default Navigation