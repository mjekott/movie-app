import React from 'react'

type TitleProps = {
    title: string;
    subtitle:string
}

const Title = ({title,subtitle}:TitleProps) => {
  return (
      <div>
          <h1>{title}</h1>
          <p>{subtitle}</p>
    </div>
  )
}

export default Title