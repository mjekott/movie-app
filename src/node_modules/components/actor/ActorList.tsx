import React from 'react'
import ActorCard, {ActorProps} from "./ActorCard"

type ActorListProps = {
    data:ActorProps []
}

const ActorList = ({data}:ActorListProps) => {
  return (
      <div>{data.map(item => <ActorCard key={item.person.id} {...item}/>)}</div>
  )
}

export default ActorList